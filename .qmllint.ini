[General]
MaxWarnings=0

[Warnings]
# Main.qml receives AppController as a QQmlContext property. qmllint cannot
# resolve context properties statically, so keep all other warnings fatal and
# suppress only this category until the controller is registered as a QML type.
UnqualifiedAccess=disable
